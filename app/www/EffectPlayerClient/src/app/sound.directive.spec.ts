import { SoundDirective } from './sound.directive';

describe('SoundDirective', () => {
  it('should create an instance', () => {
    const directive = new SoundDirective();
    expect(directive).toBeTruthy();
  });
});
